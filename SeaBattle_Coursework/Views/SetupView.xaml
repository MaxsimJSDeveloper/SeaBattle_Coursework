<UserControl x:Class="SeaBattle_Coursework.Views.SetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SeaBattle_Coursework.Views"
             xmlns:vm="clr-namespace:SeaBattle_Coursework.ViewModels">

    <!-- Створюємо наш єдиний стан гри саме тут, на етапі розстановки -->
    <UserControl.DataContext>
        <vm:GameViewModel />
    </UserControl.DataContext>

    <Grid>
        <!-- Ділимо екран на 2 рядки (дошки і кнопка) та 2 колонки (гравець 1 і 2) -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <!-- Верхня частина для дощок -->
            <RowDefinition Height="80" />
            <!-- Нижня для кнопки -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Дошка Гравця 1 -->
        <local:BoardView DataContext="{Binding Player1Board}" Grid.Row="0" Grid.Column="0" Margin="20" />

        <!-- Дошка Гравця 2 -->
        <local:BoardView DataContext="{Binding Player2Board}" Grid.Row="0" Grid.Column="1" Margin="20" />

        <!-- Кнопка переходу -->
        <Button Grid.Row="1" Grid.ColumnSpan="2" 
                Content="ПОЧАТИ БІЙ" 
                FontSize="24" FontWeight="Bold" Margin="20,10"
                Click="StartBattle_Click" />
    </Grid>
</UserControl>